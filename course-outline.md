# 定義任務，去泡杯咖啡
## Vibe Coding 實戰：從失控到讓 AI 真正幫你做事

---

## 開場：Vibe Coding 很美好

第一次用 Claude Code、Cursor 或 Copilot 的感覺：

- 描述你想要什麼
- 程式碼出現了
- 它能動
- 你感覺自己是 10x 工程師

---

## 直到遇到這些問題

### 問題 1：Bug 改不好，來回空耗

讓 AI 自己跑，它說完成了，能動，你就沒認真看它寫了什麼。

直到幾天後 bug 出現。叫它修，它說改完了，你執行之後發現改爛了，只好叫它重改。來回幾輪之後，bug 還是沒修好。

打開程式碼才發現：200 行 function、邏輯四散、零解釋——根本不知道從哪裡下手。

**解法**：不要放飛，每個程式碼片段都手動 approve 或 disapprove。

---

### 問題 2：Conventions 不一致

AI 不了解你的 codebase——命名風格、有哪些既有函式可以用、怎麼用。寫出來的東西能動，但不是你們的風格，也沒有用到已經有的東西。

**解法**：
- 把命名慣例、既有函式庫的用法寫進 `CLAUDE.md`
- 動手前先讓 AI 輸出實作計畫，你確認它用了正確的東西，才開始寫

---

## 第二輪：照著做之後，新的問題出現了

### 問題 1：手動 approve 每個片段很累

你變成 AI 的保姆，速度慢、認知負擔高。

### 問題 2：approve 了好幾輪之後才發現方向就錯了

AI 沒有事先說清楚它打算改哪些檔案、怎麼改。你一個一個 approve，到後面才發現整個實作方向就有問題，前面全部白做。

### 問題 3：CLAUDE.md 越來越肥

規範越補越多。拆分文件是一個解法，但你要決定怎麼拆、哪個任務讀哪個檔案，而且不能用軟連結，放哪裡也是問題。文件多了之後一樣難維護，而且很容易過時。

**解法**：
- 問題 1 & 2：不要在 code 層級 approve，改成在**計畫層級 approve**——讓 AI 先輸出完整計畫（改哪些檔案、怎麼改），你確認方向對了才讓它執行
- 問題 3：讓 AI 在計畫階段**自己去找相關 snippets**，從現有程式碼推斷命名慣例和既有函式，不靠文件；CLAUDE.md 只放 codebase 看不出來的東西，保持精簡

但計畫是建立在需求上的——如果需求本身就沒釐清，計畫再嚴謹也是建立在沙堆上。

#### 例子：訂單狀態通知

你說：「幫我做訂單狀態通知。」

**沒有 spec，AI 自己腦補：**

Spec（AI 假設）：
- 訂單狀態變更時寄 email 給使用者

Implementation checklist：
- [ ] 在 `Order` model 的 `status` 欄位變更時加入 callback
- [ ] 建立 `OrderNotificationMailer`，實作 `status_changed` method
- [ ] 寄送 email 至 `order.user.email`
- [ ] 寫測試確認 email 有被觸發

做完能動，但通知誰、哪些狀態、失敗怎麼處理——全部都是 AI 自己決定的。

---

**有完整 spec（PM 先釐清需求）：**

Spec：
- `pending→confirmed`、`confirmed→shipped`、`shipped→delivered` 三個狀態變更要通知
- 客戶收 email、業務收 Slack
- 退款、取消不通知
- 發送失敗不能靜默，需要被處理

Implementation checklist：
- [ ] 在 `Order` model 監聽三個指定的狀態轉換
- [ ] `email` 通知：呼叫既有 `NotificationService.send_email`，帶入對應狀態的 template key
- [ ] `slack` 通知：呼叫既有 `NotificationService.send_slack`，頻道指向業務群組
- [ ] 退款、取消狀態不進入通知流程
- [ ] 通知失敗時丟出 exception，由上層統一處理，不靜默吞掉
- [ ] 寫測試覆蓋每個狀態轉換的通知行為，以及失敗情境

---

---

## 第三輪：照著做之後，新的問題出現了

### 問題 1：計畫涵蓋不了所有面向

安全性漏洞、程式碼複用性這類東西，不可能每次都寫進計畫，但 AI 不會主動顧到。

### 問題 2：計畫太長，AI 會漏掉項目

篇幅一長，執行時就開始跳過或忽略某些部分。

### 問題 3：測試完才發現需求當初沒想清楚

開發完、跑了測試，結果發現一堆 edge case 當初根本沒考慮到。要改的不是 code，是需求本身——但這時候都已經做完了。

**解法**：
- 問題 1：把安全性、複用性這些做成 checklist，agent 必須全部完成才算結束，不能跳過
- 問題 2：獨立的 Reviewer agent 專門負責這些面向，職責單一、不容易漏
- 問題 3：在實作之前先用 PM agent 把需求問清楚，把 edge case 逼出來，而不是等到測試完才發現
  - 有 PM 的團隊：幫 PM 開出更完整的需求
  - 沒有 PM 的開發者：在動手前先把需求想清楚
  - 需求沒釐清就實作，做完發現不對打掉重來，浪費的不只是時間，token 也一起燒掉；前面多問幾個問題，後面少一輪來回，省的 token 遠比多的多
