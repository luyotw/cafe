## Convergence Review

[ ] Read the previous review and carry its complete `Finding Registry`, including closed/fixed/withdrawn lineages; build the `Correction Impact Set` from unresolved blockers plus the exact correction delta, and inspect the complete affected production boundary before classifying the one bounded candidate scan
[ ] Keep an item blocking only when it is an unresolved existing blocker lineage, a regression causally introduced by the current correction, or a newly evidenced `Impact: Critical` finding that meets the skill's reachable-path and severe-impact contract; risk or acceptance rows become blocking only by linking to one of these allowed `BLK-NNN` classes, and a late non-Critical observation cannot become blocking merely by calling a row `reopened`; do not relabel a late Important or Minor candidate as Critical because confidence is high, input is theoretically unbounded, or the first three rounds missed it
[ ] Convert every other newly discovered Important or Minor candidate to an open `FUP-NNN` proposal instead of a Todo blocker; deduplicate it against carried blocker and proposal root-cause identities, and record first-seen iteration/HEAD, impact, confidence, scope relationship, reachable production path and evidence, plus a concise draft issue title and body
[ ] If an existing FUP lineage later gains qualifying Critical evidence or becomes a regression caused by the current correction, preserve its `FUP-NNN` history, mark it `promoted` with a linked existing-or-new `BLK-NNN`, and keep that BLK open until independently closed; never downgrade a Critical blocker into an ordinary follow-up
[ ] Carry every registry entry into the current output with its original ID, root-cause identity, and first-seen evidence; mark a BLK `closed`, or a FUP `fixed` / `withdrawn`, only with current evidence, never create a GitHub issue, and complete the full Correction Impact Set plus classification of the bounded scan before handoff
