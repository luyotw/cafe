## Finalize Review

[ ] Confirm that the reviewer modified no code, then write {output_file} in this exact order: `## Review Baseline`, `## Finding Registry`, `## Todo List`, `## Follow-up Proposals`, `## Triggered Risk Coverage`, `## Acceptance Closure Evidence`, `## Outcome`; record `Review Mode: discovery | convergence` in the baseline; on a provisional pass, keep a compact `Cumulative Seam Coverage Summary` there for applicable limit compatibility, decision check-to-use, and automatic-consumer budget seams with source review and evidence HEAD; on correction review, also put the `Correction Impact Set` and compact `Carried Evidence Summary` there, keep detailed risk and acceptance rows for entries allowed to block by the active mode and correction-impacted entries classified `follow_up`, and reference unaffected evidence by stable ID, source review, evidence HEAD, and unchanged-boundary reason instead of rewriting it; make `Finding Registry` a deterministic `ID | kind | root-cause identity | first-seen iteration/HEAD | impact | confidence | current status | linked ID | evidence HEAD` table containing every historical and current BLK/FUP lineage, with BLK status `open | closed` and FUP status `open | fixed | withdrawn | promoted`; format Todo blockers as unchecked entries for every open `BLK-NNN` with root cause, production path, file path and line number, and format `Follow-up Proposals` as the open `FUP-NNN` subset with `ID | first seen iteration/HEAD | impact | confidence | scope relationship | production path and evidence | draft issue title and body | status`; preserve promoted FUP rows and their linked BLK in the registry but omit them from open proposals, say explicitly when Todo or open proposals has no entries, and identify defects without code solutions, raw unbounded output, or a manufactured verification receipt
[ ] Route missing requirement authority or required user input through the active review step's declared reactive handoff; route implementation, test, evidence, or other blocking findings to `develop`; only a fully closed exit audit proceeds to the next workflow step, then write the next-step baton and keep the response brief
