## Exit Audit

[ ] If the current result is a provisional pass, re-check the cumulative `merge-base({base_branch}, HEAD)..HEAD` change map against every fixed risk trigger on the first review; on a correction review, re-check the exact correction delta and `Correction Impact Set` against the retained baseline and add any missed affected or new obligation; if blockers already remain, record why the exit audit was skipped instead of manufacturing pass evidence
[ ] Before routing a pass, freeze the current HEAD and require every previous-open, impacted, reopened, or new row to be `closed_fresh` at that HEAD; allow an unaffected row to remain `closed_reused` only when its source review and evidence HEAD are recognizable, the previous HEAD is an ancestor, and complete-boundary inspection proves the correction did not invalidate it—otherwise reopen it and obtain fresh evidence
