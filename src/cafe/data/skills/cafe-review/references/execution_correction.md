## Correction Review

[ ] Read the previous review, its baseline and matrices, every prior finding, and the exact correction delta first; re-verify each prior blocker and corrected root cause through its callers, consumers, supported modes, aliases, cardinalities, lifecycle paths, tests, and fixtures, consolidating sibling violations in one pass
[ ] Build the `Correction Impact Set`: start with every previous open finding, every previous open row, and every changed path or symbol in `Previous Reviewed HEAD..Reviewed HEAD`, then map each complete boundary across production entries and consumers, transitive calls, configuration/default/precedence, schema and durable lifecycle, identity/authorization/path/lock/snapshot inputs, dependency/platform wrappers, probes, tests, fixtures, and mocks; reopen a prior row when any boundary or authoritative requirement changed, a new route or trigger appeared, the previous HEAD is unusable, or bounded reverse caller/consumer inspection cannot prove it unaffected—direct file byte equality alone is insufficient
[ ] Review only the `Correction Impact Set` and any new candidate findings without restarting an unrelated repository-wide audit; obtain current-HEAD evidence for every previous blocker and impacted, reopened, or new row, but carry a prior closed row as `closed_reused` when its complete recorded boundary is proven unchanged, retaining a compact reference to its source review and evidence HEAD instead of rerunning its probe or rewriting its evidence
