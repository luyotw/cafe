## Correction Review

[ ] Read the previous review, its baseline and matrices, every prior finding, and the correction delta first; re-verify each finding and corrected root cause through its callers, consumers, supported modes, aliases, cardinalities, lifecycle paths, tests, and fixtures, consolidating sibling violations in one pass
[ ] Before reusing evidence, map its complete boundary: production entry and consumer, transitive calls, configuration/default/precedence, schema and durable lifecycle, identity/authorization/path/lock/snapshot inputs, dependency/platform wrapper, plus the supporting probe, test, fixture, and mock; mark it stale when any boundary or authoritative requirement changed, a new route or trigger appeared, the previous HEAD is unusable, or bounded reverse caller/consumer inspection cannot prove it unaffected—direct file byte equality alone is insufficient
[ ] Review the correction and every boundary it invalidates without restarting an unrelated repository-wide audit; ordinary acceptance evidence may become `closed_reused` only when its full boundary is proven unchanged, while triggered risk evidence may be reused for diagnosis but must become current-HEAD `closed_fresh` before a pass
